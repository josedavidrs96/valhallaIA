export interface ClassType {
  id: string
  name: string
  slug: string
  color: string
}

export interface Coach {
  id: string
  user_id: string
  first_name: string
  last_name: string
  email: string
}
